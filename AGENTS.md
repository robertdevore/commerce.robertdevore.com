# Agent guide

This is a copyable consumer, not Commerce implementation. Keep dependencies
pinned, products under `content/shop`, and Mock checkout visibly labeled. Run
`npm run validate`, `npm run build`, and the clean-clone test before publishing.
